{
  "name": "@vdf/shared-types",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest"
  },
  "peerDependencies": {
    "@nestjs/mongoose": "^10.0.0",
    "mongoose": "^8.0.0"
  },
  "devDependencies": {
    "@nestjs/mongoose": "^10.0.0",
    "mongoose": "^8.0.0",
    "class-validator": "^0.14.0",
    "reflect-metadata": "^0.1.13"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "rootDir": "src"
  }
}
