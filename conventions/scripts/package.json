{
  "name": "sync-conventions-runtime-scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "validate:executor-routes": "tsx validate-executor-routes.ts",
    "compile:authority-topology": "tsx compile-authority-topology.ts",
    "validate:authority-topology": "tsx validate-authority-topology.ts",
    "validate:references": "tsx validate-references.ts",
    "validate:changed-files": "tsx validate-changed-files.ts",
    "compile:runtime-artifact-topology": "tsx compile-runtime-artifact-topology.ts",
    "validate:runtime-artifacts": "tsx validate-runtime-artifacts.ts",
    "generate:phase-bundle": "tsx generate-phase-bundle.ts",
    "validate:phase-bundle": "tsx validate-phase-bundle.ts",
    "verify:scripts": "tsx verify-scripts.ts"
  },
  "dependencies": {
    "yaml": "^2.4.5"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.4",
    "@types/node": "^20.14.10"
  }
}
