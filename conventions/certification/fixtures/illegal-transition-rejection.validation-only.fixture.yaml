version: '1.0'
file:
  name: illegal-transition-rejection.validation-only.fixture.yaml
  path: certification/fixtures/illegal-transition-rejection.validation-only.fixture.yaml
  role: dry_run_scenario_fixture
  executability: fixture
purpose: Provide a concrete adapter-neutral dry-run certification fixture for the illegal_transition_rejection
  scenario using non-mutating transition-control artifact presence validation only.
ownership:
  owns:
  - illegal_transition_rejection_dry_run_fixture
  - negative_transition_control_certification_input
  does_not_own:
  - production_source_mutation
  - real_mission_controller_transition_decision
  - real_transition_legality_decision
  - scenario_enforcement_decision
  - language_or_framework_specific_test_rules
sections:
  fixture_identity:
    fixture_id: illegal_transition_rejection_minimal_controller_block_only
    scenario_id: illegal_transition_rejection
    mission_profile: illegal_transition_rejection
    execution_mode: artifact_presence_validation
  fixture_boundaries:
    mutation_allowed: false
    source_mutation_allowed: false
    language_neutral: true
    framework_neutral: true
    adapter_outputs_required: false
    real_transition_legality_provenance_claimed: false
    real_mission_controller_block_provenance_claimed: false
  expected_certification_behavior:
    rollout_mode: observe
    enforcement_mode: observe
    incomplete_evidence_is_warning: true
    blocking_decision_expected: false
    negative_control_shape_expected: illegal_transition_rejected_without_state_mutation
  required_evidence:
  - evidence_type: transition_legality_result
    accepted_paths:
    - .ai/validation/validate-transition-legality.result.yaml
  - evidence_type: mission_controller_block
    accepted_paths:
    - .ai/validation/advance-mission.result.yaml
  fixture_evidence_artifacts:
    materialization_mode: explicit_route_argument_only
    materialization_argument: --materialize-fixture-evidence
    generated_by: run-dry-run-certification
    mutation_allowed: false
    artifacts:
    - evidence_type: transition_legality_result
      path: .ai/validation/validate-transition-legality.result.yaml
      artifact: transition_legality_result
    - evidence_type: mission_controller_block
      path: .ai/validation/advance-mission.result.yaml
      artifact: mission_controller_block
  validation_contract:
    route_id: run_dry_run_certification
    required_result_artifact: .ai/certification/dry-run-certification.yaml
    required_report_artifact: .ai/reports/dry-run-certification-report.yaml
    fixture_status_when_shape_valid: fixture_valid
