package: sync_conventions_system_executor_pivot_reconciled
generated_from: Executor Pivot Reconciliation Pass applied to accepted phase bundle routing milestone.
yaml_file_count: 149
folder_count: 23
notes:
- Milestone baseline was conventions_phase_bundle_routes.zip.
- Central executor routing with local script requirements is the active runtime automation model.
- Agents must not call scripts directly; scripts are resolved only through declared executor routes.
- No standalone compiler, validation, scripts convention, manual authority registry, or manual authority boundary subsystem
  is active.
- Compiler and validation behaviors are executor route types backed by centralized scripts/ implementation files.
- Phase bundles are generated and validated through executor routes before phase-agent consumption.
- Executor pivot reconciliation removed stale authority/script convention references and wired local lifecycle hooks to required_executor_routes.
executor_system_summary:
  executor_entrypoint: executors/conventions.executors.yaml
  central_route_file: executors/conventions.executor-routes.yaml
  route_namespace: global_with_optional_subsystem_prefix
  route_grouping: by_executor_type
  route_naming: verb_noun
  execution_order: declared_order
  unknown_route_behavior: fail_closed
  script_folder: scripts/
  script_folder_meaning: centralized runnable implementation path only
  direct_script_calls_by_agents: forbidden
reconciliation_summary:
  stale_authority_registry_references_removed: true
  stale_script_convention_references_removed: true
  core_validation_delegates_to_executor_routes: true
  runtime_artifact_validation_route_added: true
  local_required_executor_route_files:
  - execution/conventions.execution.yaml
  - gates/conventions.gates.yaml
  - handoffs/conventions.handoffs.yaml
  - missions/conventions.missions.yaml
  - multi-agent/conventions.multi-agent.yaml
  - multi-agent/conventions.phase-bundle-policy.yaml
  - phases/conventions.phase-lifecycle.yaml
  - reports/conventions.reports.yaml
  - revisions/conventions.revisions.yaml
  local_required_executor_route_file_count: 9
foundational_routes:
- collect_phase_report
- compile_authority_topology
- compile_runtime_artifact_topology
- create_revision_task
- generate_mission_bundle
- generate_phase_bundle
- prepare_agent_handoff
- run_gate_check
- validate_authority_topology
- validate_changed_files
- validate_executor_routes
- validate_phase_bundle
- validate_phase_output
- validate_references
- validate_runtime_artifacts
