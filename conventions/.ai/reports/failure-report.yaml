report_id: "failure-report"
phase_id: post_implementation_validation
schema_version: "1.0"
status: complete

summary: >
  No blocking failures found during post-implementation validation.
  All behavior contract requirements satisfied structurally.
  Two non-blocking advisories logged (uuid dep, @nestjs/axios dep).

blocking_failures: []

non_blocking_findings:
  - finding_id: NB-001
    severity: advisory
    type: dependency_addition
    description: uuid added to vbs-service dependencies (scan_id generation)
    action_taken: Added to packages/vbs-service/package.json

  - finding_id: NB-002
    severity: advisory
    type: dependency_addition
    description: "@nestjs/axios added to all services (TerminusModule requirement)"
    action_taken: Added to packages/{vss,vbs,sgs}-service/package.json

  - finding_id: NB-003
    severity: note
    type: integration_test_pending
    description: TO-005 and TO-015 integration tests are structural stubs
    action_required: Complete after npm install and real-infrastructure provisioning

failure_classification: no_failures
repair_required: false

produced_outputs:
  - .ai/reports/failure-report.yaml
