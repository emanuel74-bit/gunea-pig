# /transition-phase

Use the `transition-phase` skill to run gate, handoff, mission-controller `advance_mission` observation, next bundle generation, or revision fallback.

Do not bypass executor routes, manually mark phases complete, manually author transition artifacts, or infer mission advancement.
